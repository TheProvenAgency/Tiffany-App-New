{
  "name": "msfs-dashboard",
  "version": "1.0.0",
  "description": "Ms. Financial Solutions — Business Command Center (GHL + Fanbasis + DisputeFox + Meta)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^4.19.2",
    "pg": "^8.13.1"
  }
}
